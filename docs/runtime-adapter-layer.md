# Runtime Adapter Layer

The MVP runtime adapter is Humanize local shell plus agent-runner, command guard, and snapshot manager.

Hermes is an optional future adapter reference. Hermes should not own paper semantics; paper-repro-core defines evidence maps, decomposition, criteria, checkpoints, and result comparison.
