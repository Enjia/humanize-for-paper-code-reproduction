# Inference Optimization Paper Example

Use paper type `inference-optimization` for latency, throughput, memory, kernel, warmup, and profiling claims. Training artifacts remain not applicable unless paper evidence includes training claims.
