# ML Training Paper Example

Use paper type `ml-training` when the paper has training, finetuning, optimizer, epoch, or checkpoint evidence. Required artifacts include dataset preparation, training or finetuning, evaluation script, environment spec, reproduce entrypoint, and results.json.
