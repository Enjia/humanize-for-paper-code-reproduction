# Paper Workspace Contract

The workspace root defaults to `paper-repro/<slug>`. Source files, environment files, plan files, reproduce.sh, results.json, and reports are deliverables.

Large generated outputs belong under workspace `outputs/` and should be referenced by path, hash, and summary in checkpoint evidence.
