# Paper Repro Core

paper-repro-core owns domain contracts: evidence map, paper decomposition, criteria, artifact profiles, checkpoint graph, task lineage, results comparison, and final package audit.

Runtime adapters execute agents and commands but do not redefine these semantics.
