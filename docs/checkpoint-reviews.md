# Checkpoint Reviews

Child checkpoints require one reviewer verdict. Parent checkpoints require two independent reviewer runs with distinct run IDs and separate output artifacts.

Reasonable non-conflicting findings are carried into the next prompt. Conflicting findings create an arbitration task.
