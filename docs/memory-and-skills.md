# Memory And Skills

Memory is disabled by default for MVP. When enabled, only redacted event records are persisted. Raw logs, transcripts, secrets, and full paper text are not stored.

Skill candidates are disabled by default. Candidate skills stay under `.humanize/skills/candidates/` and require scanner validation plus manual review before promotion.
