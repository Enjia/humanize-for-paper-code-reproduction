# Provider Roles

Provider roles are configured per responsibility: worker, summary reviewer, code reviewer, planner A, planner B, synthesizer, memory, and skill review.

Explicit provider config wins. Model-name detection is only a fallback when provider is absent. Missing dependency errors must name the role and provider.
